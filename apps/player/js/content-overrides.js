/* ============ KODEX OS — применение правок Studio ============ *
 * Studio (apps/studio) хранит созданные/изменённые методистом дела через
 * общий сервис content-api (packages/game-data/merge.js) — это сетевой
 * вызов, поэтому асинхронный. Здесь, до старта остальной логики (state.js,
 * app.js), эти правки накладываются на CASES «на месте» — весь остальной
 * код Player не знает и не должен знать, что часть дел пришла не из сида,
 * а из Studio. app.js обязан дождаться window.__CONTENT_READY__ перед
 * первым route().
 * ------------------------------------------------------------------- */
'use strict';

window.__CONTENT_READY__ = applyStudioOverrides(CASES);
