/* ============ KODEX OS — применение правок Studio ============ *
 * Studio (apps/studio) хранит созданные/изменённые методистом дела в
 * localStorage (packages/game-data/merge.js). Здесь, сразу после загрузки
 * сида CASES из data.js и до старта остальной логики (state.js), эти правки
 * накладываются на CASES «на месте» — весь остальной код Player не знает
 * и не должен знать, что часть дел пришла не из сида, а из Studio.
 * ------------------------------------------------------------------- */
'use strict';

applyStudioOverrides(CASES);
